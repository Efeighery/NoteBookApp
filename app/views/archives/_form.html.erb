<%= form_with(model: archive) do |form| %>
  <% if archive.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(archive.errors.count, "error") %> prohibited this archive from being saved:</h2>

      <ul>
        <% archive.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :full_name, style: "display: block" %>
    <%= form.text_field :full_name %>
  </div>

  <div>
    <%= form.label :note_title, style: "display: block" %>
    <%= form.text_field :note_title %>
  </div>

  <div>
    <%= form.label :note_space, style: "display: block" %>
    <%= form.rich_text_area :note_space %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
